# FastAPI + React Basic Boilerplate

This project is a simple boilerplate for creating a full-stack application using FastAPI for the backend and React (with Vite) for the frontend.

## Project Structure